{{#if this.image_url}}
    <img src="{{this.image_url}}"  alt="beersImages"/>
    {{/if}}
    <div class="beersDisplayed">
    <h1>{{this.name}}</h1>
    <p>{{this.description}}</p>
    <p>{{this.tagline}}</p>
    </div>
 